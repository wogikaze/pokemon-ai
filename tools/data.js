const a = {
    "名前": new Tokusei("名前", { "out": (attacker, defender) => { } })

}

if (sort_turn(a_poke, b_poke)) { [b_poke, a_poke,] = activateItem(b_poke, a_poke, phaze) }
else { [a_poke, b_poke] = activateTokusei(a_poke, b_poke, phaze) }